
$('#myTable tbody').append(
`
<% @posts.each do |post| %>
 <tr>
        <td><%= post.id %></td>
        <td><%= post.title %></td>
        <td><%= truncate post.content, length:20 %></td>
        <td><%= post.content %></td>
        <td><%= link_to 'Show', post %>|</td>
        <td><%= link_to 'Edit', edit_post_path(post) %>|</td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
<% end %>
`
);
alert("무한스크롤");
console.log("done");